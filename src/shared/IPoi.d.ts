/**
 * Note: O.L : This interface is temporal, until we shall decide on an actual POI and what we want from it.
 */
export interface IPoi {
  id: string;
  name: string;
  xRotation: number;
  yRotation: number;
}
